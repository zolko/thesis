package hu.bme.thesis.dsl.sensors.queries

import "http://www.bme.hu/thesis/dsl/sensors/SensorsDsl"

pattern sensors(sensor: Sensor) {
	Sensor(sensor);
}

pattern messages(message: Message) {
	Message(message);
}

pattern model(model: Model) {
	Model(model);
}