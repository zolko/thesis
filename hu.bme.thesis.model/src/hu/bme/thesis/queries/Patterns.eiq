package hu.bme.thesis.queries

import "model.meta"

pattern sensors(sensor: Sensor) {
	Sensor(sensor);
}

pattern sensorsWithMqtt(sensor: Sensor, setup: MqttSetup) {
	Sensor(sensor);
	MqttSetup(setup);
}